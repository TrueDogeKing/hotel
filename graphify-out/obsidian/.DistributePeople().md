---
source_file: "src/CampCenter.Application/Services/RoomMixCalculator.cs"
type: "code"
community: "Room Mix Calculator Tests"
location: "L104"
tags:
  - graphify/code
  - graphify/EXTRACTED
  - community/Room_Mix_Calculator_Tests
---

# .DistributePeople()

## Context

_Source: `src/CampCenter.Application/Services/RoomMixCalculator.cs` (defined near L104; showing L102–L126 of 126)._

```csharp
    /// Distributes people across the selected rooms: every room at capacity except
    /// the last, which takes the remainder.
    public static List<(int Capacity, int PeopleCount)> DistributePeople(
        int headcount,
        IReadOnlyDictionary<int, int> counts
    )
    {
        var rooms = counts
            .Where(kv => kv.Value > 0)
            .OrderByDescending(kv => kv.Key)
            .SelectMany(kv => Enumerable.Repeat(kv.Key, kv.Value))
            .ToList();

        var result = new List<(int, int)>();
        var remaining = headcount;
        foreach (var capacity in rooms)
        {
            var take = Math.Min(capacity, remaining);
            result.Add((capacity, take));
            remaining -= take;
        }

        return result;
    }
}
```

## Connections
- [[.AssignRooms()]] - `calls` [EXTRACTED]
- [[.DistributePeople_FillsAllButLastRoom()]] - `calls` [EXTRACTED]
- [[Capacity]] - `references` [EXTRACTED]
- [[IReadOnlyDictionary]] - `references` [EXTRACTED]
- [[List_9]] - `references` [EXTRACTED]
- [[PeopleCount]] - `references` [EXTRACTED]
- [[RoomMixCalculator]] - `method` [EXTRACTED]

#graphify/code #graphify/EXTRACTED #community/Room_Mix_Calculator_Tests