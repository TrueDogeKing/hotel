---
source_file: "src/CampCenter.Infrastructure/Persistence/Configurations/RoomConfiguration.cs"
type: "code"
community: "Room Management"
location: "L9"
tags:
  - graphify/code
  - graphify/EXTRACTED
  - community/Room_Management
---

# .Configure()

## Context

_Source: `src/CampCenter.Infrastructure/Persistence/Configurations/RoomConfiguration.cs` (defined near L9; showing L7–L29 of 29)._

```csharp
public class RoomConfiguration : IEntityTypeConfiguration<Room>
{
    public void Configure(EntityTypeBuilder<Room> builder)
    {
        builder.ToTable("Rooms");

        builder.HasKey(x => x.Id);

        builder.Property(x => x.Number).IsRequired().HasMaxLength(32);

        builder.HasIndex(x => x.Number).IsUnique();

        builder.Property(x => x.Description).HasMaxLength(512);

        // Optimistic concurrency via the PostgreSQL "xmin" system column.
        builder
            .Property(x => x.RowVersion)
            .HasColumnName("xmin")
            .HasColumnType("xid")
            .ValueGeneratedOnAddOrUpdate()
            .IsConcurrencyToken();
    }
}
```

## Connections
- [[EntityTypeBuilder_6]] - `references` [EXTRACTED]
- [[Room_1]] - `references` [EXTRACTED]
- [[RoomConfiguration]] - `method` [EXTRACTED]

#graphify/code #graphify/EXTRACTED #community/Room_Management