---
source_file: "src/CampCenter.Application/Services/AdminBookingService.cs"
type: "code"
community: "Admin Booking & Notifications"
location: "L248"
tags:
  - graphify/code
  - graphify/EXTRACTED
  - community/Admin_Booking__Notifications
---

# .GetDashboardAsync()

## Context

_Source: `src/CampCenter.Application/Services/AdminBookingService.cs` (defined near L248; showing L246–L293 of 346)._

```csharp
                    hit.Assignment?.PeopleCount,
                    closed,
                    closureReason,
                    openTasks.GetValueOrDefault(room.Id)
                );
            })
            .ToList();

        return new OccupancyDto(
            start,
            end,
            rooms.Where(r => r.IsActive).Sum(r => r.Capacity),
            roomDtos.Sum(r => r.PeopleCount ?? 0),
            roomDtos
        );
    }

    public async Task<DashboardDto> GetDashboardAsync(CancellationToken cancellationToken = default)
    {
        var today = DateOnly.FromDateTime(DateTime.UtcNow);

        var upcoming = await _bookings.ListUpcomingAsync(today, 8, cancellationToken);
        var upcomingDtos = upcoming
            .Select(b => new DashboardBookingDto(
                b.Id,
                b.OrganizationName,
                b.StartDate,
                b.EndDate,
                b.Headcount,
                b.RoomAssignments.Sum(a => a.PeopleCount),
                b.Status.ToString()
            ))
            .ToList();

        var pending = await _bookings.ListAsync(BookingStatus.PendingDeposit, cancellationToken);
        var confirmed = await _bookings.ListAsync(BookingStatus.Confirmed, cancellationToken);
        var paid = await _bookings.GetCompletedPaymentKindsAsync(
            confirmed.Select(b => b.Id).ToList(),
            cancellationToken
        );
        var overdue = confirmed.Count(b =>
            !(paid.GetValueOrDefault(b.Id) ?? []).Contains(PaymentKind.Final)
            && b.StartDate.AddDays(-_settings.FinalPaymentDueDays) < today
        );

        var activeClosures = (await _closures.GetAllAsync(cancellationToken)).Count(c =>
            c.EndDate >= today
        );
```

## Connections
- [[.CountOpenAsync()]] - `calls` [INFERRED]
- [[.GetActiveAsync()]] - `calls` [INFERRED]
- [[.GetCompletedPaymentKindsAsync()]] - `calls` [INFERRED]
- [[.ListAsync()_4]] - `calls` [INFERRED]
- [[AdminBookingService]] - `method` [EXTRACTED]
- [[CancellationToken_22]] - `references` [EXTRACTED]
- [[DashboardDto]] - `references` [EXTRACTED]
- [[Task_21]] - `references` [EXTRACTED]

#graphify/code #graphify/EXTRACTED #community/Admin_Booking__Notifications