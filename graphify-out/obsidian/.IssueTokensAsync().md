---
source_file: "src/CampCenter.Application/Services/AuthService.cs"
type: "code"
community: "Auth Service & Tokens"
location: "L129"
tags:
  - graphify/code
  - graphify/EXTRACTED
  - community/Auth_Service__Tokens
---

# .IssueTokensAsync()

## Context

_Source: `src/CampCenter.Application/Services/AuthService.cs` (defined near L129; showing L127–L165 of 165)._

```csharp
    }

    private async Task<AuthResult> IssueTokensAsync(
        AdminUser admin,
        CancellationToken cancellationToken
    )
    {
        var access = _tokenService.CreateAccessToken(admin);
        var refresh = _tokenService.GenerateRefreshToken();

        await _refreshTokens.AddAsync(
            CreateTokenEntity(admin.Id, refresh, DateTime.UtcNow),
            cancellationToken
        );
        await _refreshTokens.SaveChangesAsync(cancellationToken);

        return new AuthResult(
            access.Token,
            access.ExpiresAtUtc,
            admin.Login,
            refresh.RawToken,
            refresh.ExpiresAtUtc
        );
    }

    private static RefreshToken CreateTokenEntity(
        Guid adminUserId,
        RefreshTokenInfo info,
        DateTime createdAtUtc
    ) =>
        new()
        {
            Id = Guid.NewGuid(),
            AdminUserId = adminUserId,
            TokenHash = info.TokenHash,
            ExpiresAtUtc = info.ExpiresAtUtc,
            CreatedAtUtc = createdAtUtc,
        };
}
```

## Connections
- [[.AddAsync()_2]] - `calls` [INFERRED]
- [[.CreateAccessToken()]] - `calls` [INFERRED]
- [[.CreateTokenEntity()]] - `calls` [EXTRACTED]
- [[.GenerateRefreshToken()]] - `calls` [INFERRED]
- [[.LoginAsync()_1]] - `calls` [EXTRACTED]
- [[.SaveChangesAsync()_3]] - `calls` [INFERRED]
- [[AdminUser]] - `references` [EXTRACTED]
- [[AuthResult]] - `references` [EXTRACTED]
- [[AuthService]] - `method` [EXTRACTED]
- [[CancellationToken_23]] - `references` [EXTRACTED]
- [[Task_22]] - `references` [EXTRACTED]

#graphify/code #graphify/EXTRACTED #community/Auth_Service__Tokens